version: '3.9'

services:
  app:
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    restart: always

  nginx:
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    restart: always
